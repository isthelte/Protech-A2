#ifndef READ_EXP
#define READ_EXP

#define MAX_EXP_LENGTH 100

/**
 * Read the next input by user (from the standard input) to the string given.
 * This function also returns the length of the string being input.
 * @param exp the string variable to hold the input.
 * @return the length of the input.
 */
int read_exp(char *exp);

#endif
